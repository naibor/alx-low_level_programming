#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 *
 * Description: prints the alphabet, in lowercase, followed by a new line.
 *
 * Return: returns 0
 *
 */
void print_alphabet(void);

int main(void)
{
	 print_alphabet(void);

	 return (0);
}

void print_alphabet(void)
{
	int i;

	for (i = 0; i ){}
}
