#include "main.h"
/**
 * _abs - Function
 *
 * Description:  computes the absolute value of an integer
 *
 * Return: returns 0
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	} else
	{
		return (n * -1);
	}

}
