#include "main.h"
/**
 * strtow - Function
 *
 * Description: splits a string into words
 *
 * @str: parameter of type char, the string to split
 *
 * Return: returns NULL if string is empty
 */
char **strtow(char *str)
{
	if (str == NULL || str[0] == '\0')
	{
		return (NULL);
	}

return (NULL);
}
